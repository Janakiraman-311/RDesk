# Initialize hot reloading tracking

Initialize hot reloading tracking

## Usage

``` r
rdesk_hotreload_init(app_dir)
```

## Arguments

- app_dir:

  Path to the application root directory.

## Value

A list containing file paths as names and modification times (POSIXct)
as values.
